# Discord application credentials
DISCORD_TOKEN=
PUBLIC_KEY=
APP_ID=

# OpenAI
OPENAI_API_KEY=
DND_PLAYER_ASSISTANT_ID=
SAVAGE_WORLDS_ASSISTANT_ID=

# Express server (defaults to 3000 if unset)
PORT=3000

# ngrok (reserved/static domain so the public URL is stable across restarts)
NGROK_AUTHTOKEN=
NGROK_DOMAIN=
